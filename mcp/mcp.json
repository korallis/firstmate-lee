{
  "mcpServers": {
    "firstmate": {
      "type": "stdio",
      "command": "npm",
      "args": ["run", "start", "--prefix", "${workspaceFolder}/mcp"],
      "env": {
        "FM_HOME": "${env:FM_HOME}"
      },
      "envFile": "${workspaceFolder}/.env"
    }
  }
}
